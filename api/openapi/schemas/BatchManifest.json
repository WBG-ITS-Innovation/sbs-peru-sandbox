{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Manifest part of a ``POST /v1/batches`` multipart upload (ADR 0034).\n\nTier 2 batch upload is a one-shot multipart request: the manifest JSON\nand the CSV file ride together. The institution_id is *not* in the\nmanifest — the server reads it from the OAuth token and the mTLS\nsubject, both of which are already authenticated. The file name is\ncarried in the multipart `Content-Disposition: filename=` header, not\na manifest field. Both reductions remove the redundant fields the\nPrompt 5/6 metadata-only scaffold required.",
  "properties": {
    "checksum_sha256": {
      "description": "Lowercase hex SHA-256 of the CSV file bytes the institution is uploading. The server recomputes on receipt and rejects with BATCH_CHECKSUM_MISMATCH on disagreement. Also the body-hash input for the HMAC canonical request per ADR 0027 amendment (multipart body-hash definition: CSV bytes only).",
      "examples": [
        "cafebabecafebabecafebabecafebabecafebabecafebabecafebabecafebabe"
      ],
      "pattern": "^[a-f0-9]{64}$",
      "title": "Checksum Sha256",
      "type": "string"
    },
    "reporting_period_end": {
      "description": "Inclusive end of the reporting period covered by this batch.",
      "format": "date",
      "title": "Reporting Period End",
      "type": "string"
    },
    "reporting_period_start": {
      "description": "Inclusive start of the reporting period covered by this batch.",
      "format": "date",
      "title": "Reporting Period Start",
      "type": "string"
    },
    "row_count_submitted": {
      "description": "Number of complaint rows the institution claims are in the CSV. The server records this for cross-check; the worker counts the actual rows during processing.",
      "maximum": 1000000,
      "minimum": 1,
      "title": "Row Count Submitted",
      "type": "integer"
    },
    "schema_version": {
      "anyOf": [
        {
          "pattern": "^v\\d+\\.\\d+\\.\\d+$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional. Anexo 1-A schema version the institution is targeting. Defaults to the server's current schema_version when omitted. Specifying it explicitly lets the server reject a batch built against a different schema with a clearer error than per-row validation failures.",
      "examples": [
        "v0.1.0"
      ],
      "title": "Schema Version"
    }
  },
  "required": [
    "reporting_period_start",
    "reporting_period_end",
    "row_count_submitted",
    "checksum_sha256"
  ],
  "title": "BatchManifest",
  "type": "object"
}

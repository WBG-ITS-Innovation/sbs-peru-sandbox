"""Canonical Annex 1-A code lists (YAML data files).

Each ``*.yaml`` file carries a ``codes:`` list and a documentation
header naming the spec source (typically ``annexo.pdf`` /
Res. SBS 04036-2022) and a ``review_status:`` flag for any list
that still needs explicit sign-off from the SBS taxonomy reviewers.
"""

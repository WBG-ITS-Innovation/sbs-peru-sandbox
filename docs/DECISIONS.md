# Decisions Log

One line per non-trivial decision. Date | decision | rationale.

## Format

YYYY-MM-DD | [PartN] decision summary | one-line rationale.

## Entries

2026-05-16 | [Part1] Retired `scripts/requirements-harness.txt`; harness deps live in root `pyproject.toml` `dev` group; install via `uv sync` | uv project initialization (Prompt 3). See ADR 0021.
2026-05-17 | [Part1] Locked May 25 sprint kickoff scope: Parts 2/3/4 full, Parts 5/6/7/8/11 reduced, Parts 9/10 deferred entirely; PLAN.md restructured with per-Part May 25 scope subsections | May 25 sprint kickoff scope-lock (Prompt 4). See ADR 0025.
2026-05-18 | [Part2] Anexo 1-A May 25 sandbox uses a curated 15-field subset reconciled against Resolución SBS N° 04036-2022; full code lists + PII fields deferred to Part 11 | Prompt 5. See ADR 0026.
2026-05-18 | [Part2] OpenAPI 3.1 specification at api/openapi/sbs-api-v1.yaml is the canonical contract; Pydantic v2 models implement it; JSON Schemas exported; match-test enforces alignment | Prompt 5. See ADR 0027.
2026-05-18 | [Part2] RFC 9457 problem+json is the SBS error envelope; placeholder `type` namespace pending SBS sign-off; error catalog committed at api/openapi/error-catalog.md | Prompt 5. Tracked in docs/sessions/2026-05-18-prompt-05-open-questions.md.
2026-05-18 | [Part2] Developer portal uses Stoplight Elements (CDN) with Redoc 2.x as documented fallback; vendoring deferred to Part 7 / Prompt 9 | Prompt 5. See docs/research/2026-05-18-prompt-05-stack-validation.md §E.
2026-05-18 | [Part2] FastAPI application structure locked: factory pattern, ProblemDetail via FastAPI exception handlers (not middleware), middleware order body_size_limit → traceparent → correlation_id, OTel owns trace context, FastAPI auto-generated openapi/docs/redoc disabled, cursor pagination opaque base64 of `(received_at, complaint_id)`, explicit resolution_status state machine, UUID v7 via uuid-utils | Prompt 6. See ADR 0028.
2026-05-18 | [Part2] Idempotency-Key policy locked: 24-hour TTL, body-hash on store, 409 on reuse with different body, `Idempotency-Replayed: true` on cache hit, supported on POST and PATCH | Prompt 6. See ADR 0029.
2026-05-18 | [Part2] Health probe semantics locked: three probes (live, ready, startup) matching Kubernetes model; ready has 1s DB-ping cache; startup checks alembic_version presence (not exact head) | Prompt 6. See ADR 0030.
2026-05-18 | [Part2] Part 2 closed: FastAPI scaffold serves the canonical OpenAPI contract; smoke test asserts behavioral contract (ETag round-trip, idempotency replay, tenant binding, state machine, ProblemDetail, body size limit) against the running app | Prompt 6.
2026-05-19 | [Part3] mTLS client authentication contract locked: CN-as-identifier; chain + expiry + revocation validation; `direct` (sandbox) and `proxy` (production) operating modes; `MtlsSubject` carries cert thumbprint into the OAuth cert-binding check | Prompt 7 A0. See ADR 0031.
2026-05-19 | [Part3] OAuth 2.0 client_credentials contract locked: four scopes (`complaints:write/read`, `batch:upload`, `status:read`); 15-minute JWT TTL; cert-bound via RFC 8705 `cnf.x5t#S256`; no refresh tokens, no introspection in sandbox | Prompt 7 A0. See ADR 0032.
2026-05-19 | [Part3] Per-institution rate limiting contract locked: Redis token bucket, two tiers (`large` 1000/min, `small` 100/min) with per-institution override; four `X-RateLimit-*` headers on every authenticated response; 429 ProblemDetail | Prompt 7 A0. See ADR 0033.
2026-05-19 | [Part3] ADR 0027 amended: full HMAC canonical request contract (method/target/timestamp/body-hash/institution_id), `hmac-sha256-v1` algorithm prefix, 5-minute clock skew, 24-hour replay window, per-institution secret with rotation grace | Prompt 7 A0. See ADR 0027 §Amendments.
2026-05-19 | [Part3] ADR 0028 amended ×4: middleware ordering reversed for 413 path so traceparent/correlation_id bind before body-size check; bounded chunked-read on slow path; §Consequences wording narrowed to FastAPI `BaseHTTPMiddleware`; cursor pagination HMAC-signed (closes issue #L) | Prompt 7 A0. See ADR 0028 §Amendments.
2026-05-19 | [Part3] ADR 0029 amended ×2: concurrent-duplicate-POST policy uses placeholder-INSERT under unique constraint with 50ms×3 retry; replay-header safelist enumerated explicitly (Date/Server/trace recomputed, ETag/Location/Content-Type replayed) | Prompt 7 A0. See ADR 0029 §Amendments.
2026-05-19 | [Part3] ADR 0003 (api-authentication) marked Superseded by ADRs 0031/0032/0033 + the 0027 HMAC amendment — Proposed-only ADR replaced before any code referenced it; the three concerns split into separate ADRs for cleaner review surfaces | Prompt 7 A0. See ADR 0003 row in docs/adr/README.md.
2026-05-19 | [Part3] Comparator file §5.A.M added: regulator-domain precedent for the mTLS + OAuth + HMAC + rate-limiting chain (Open Banking UK, RFC 8705, AWS SigV4 §Task 1, Stripe rate-limits) | Prompt 7 A0. See docs/research/market-comparators.md §5.A.M.
2026-05-20 | [Part7] Developer portal serving mechanism locked: Stoplight Elements vendored locally to `vendor/stoplight-elements/` (no CDN); FastAPI serves the portal at `/v1/portal/` and `/v1/portal/assets/{filename}` via a path-parameter route with `VENDOR_ASSET_ALLOWLIST` (not `StaticFiles`); both routes public (`security: []`); Stoplight "Try It" disabled (mTLS cannot be satisfied from a browser) | Prompt 9 A0. See ADR 0037.
2026-05-20 | [Part7] SDK helper scope locked: Python (pure stdlib) + TypeScript (dual ESM+CJS) hand-maintained webhook verification helpers; Java + Go reference snippets (~30 lines each, CI-tested); OpenAPI Generator recipes at `v7.10.0` for long-tail languages (Java, csharp-netcore, Go); distribution exclusively via standards pack tarball at v0.1, PyPI/npm publication deferred to post-sprint | Prompt 9 A0. See ADR 0038.
2026-05-20 | [Part7] Standards pack v0.1 distribution locked: versioned tarball `standards-pack-v0.1.0.tar.gz` with `checksums.sha256` attached to GitHub release; manifest declares provenance fields including SPDX-conforming `LicenseRef-sandbox-pending-legal-review` placeholder until WBG legal sign-off; integrity via SHA-256, authenticity (SLSA + cosign) deferred to Part 11 with OCI distribution | Prompt 9 A0. See ADR 0039.
2026-05-20 | [Part7] ADR 0028 amended: portal routes (`/v1/portal/`, `/v1/portal/assets/{filename}`) added to the v1 surface; both public with `security: []`; §6 amended so the canonical contract artifacts reachable from a running app are the YAML at `/v1/openapi.yaml` plus the rendered portal at `/v1/portal/` (view onto the same YAML); allowlist-not-resolve serving via path-parameter route returns 404 on misses, consistent with the institution-binding defensive 404 posture | Prompt 9 A0. See ADR 0028 §Amendments.
2026-05-20 | [Part7] Comparator file §5.A.P + §5.A.S + §5.A.D added: regulator-domain precedent for portal-serving (Open Banking UK / Brazil Open Finance / Australian CDR self-hosted portals), SDK helper distribution (Stripe narrow-then-expand, OpenAPI Generator for long-tail), standards-pack distribution and provenance (GitHub release pattern with SLSA + cosign upgrade path) | Prompt 9 A0. See docs/research/market-comparators.md §5.A.P, §5.A.S, §5.A.D.
2026-05-24 | [P11A] Added sandbox demo ingestion endpoint POST /v1/internal/demo/simulate-submission backing the LiveIngestionPanel; deterministic regex+allowlist PII redactor (`pii-redaction-demo-v1`); deterministic data-quality checker (`dq-demo-v1`); new `raw_complaints` table is the only PII-bearing store; agent_runs row written with `tool_name="anonymizer"` (DQ in `final_output.data_quality` to avoid widening the JSON Schema enum) | Mission overlay on top of Part 11. Production-shaped POST /v1/complaints auth chain unchanged. See ADR 0044, ADR 0045.
2026-05-25 | [P11A.5] Added external sandbox granular complaint API POST /v1/sandbox/complaints/granular and CLI institution sender scripts/institution_push_demo.py; reuses the P11A redaction/DQ/audit/SSE orchestrator end-to-end and the full institutional security chain (mTLS subject → OAuth `complaints:write` → HMAC SHA-256 → Idempotency-Key); returns a structured institution receipt; raw PII remains confined to `raw_complaints`; existing /v1/complaints production-shaped route unchanged | Institution-to-SBS sandbox connection so a synthetic institution can post a real-shaped complaint over a real signed connection. See docs/demo/institution-api-workflow.md.
2026-05-27 | [Part12] Agent layer — three-layer architecture (agents/tools/supervisors) locked: five agents (`triage`, `investigation`, `synthesis` real; `taxonomy-harmonizer`, `cross-source-correlator` scaffolded), ten registered tools, in-house tool-calling loop, four model providers (`on_prem` default with mock fallback / `replay` for demo determinism / `mock` for CI / `cloud` gated NotImplementedError). Additive schema extensions: `agent_runs.status` gains `in_progress` (migration 20260527_0001_agents_status); `tool_name` enum extended from 5 to 15 names. Findings detail gains `executive_summary`, `anomaly`, `similar_complaints`; cockpit gains `agent_stats` block. Synchronous in-session dispatch — async via arq deferred to v0.2. | See ADR 0001, docs/demo/2026-05-27-agents-ready.md, tests/integration/test_agent_pipeline.py.
2026-05-27 | [P-RESHAPE-1] Triage now emits `system_signal: bool` + `system_signal_reasons: list[str]` from a deterministic detector (outage / fraud / amount > 50k PEN with fraud motivo / FI breach indicator). Investigation is gated on `system_signal == true`; the per-complaint `route_to == "investigation"` invocation path is removed (route_to is informational only). Conftest seed: BCO-2026-000001 narrative changed to the locked outage example so the demo invariants test exercises the new gate. Reason codes are enum-only — no narrative excerpts leak. | May-2026 cockpit reshape — Jorge's team was being drowned in per-complaint Investigation output. See api/sbs_api/agents/triage_signals.py, tests/test_triage_system_signal.py, tests/integration/test_agent_pipeline.py.
2026-05-27 | [P-RESHAPE-2] Aggregation job (`api/sbs_api/aggregation/`: windower + detector + severity + tick) emits `pattern_detections` rows from rolling windows over the canonical complaints stream joined against a new `indecopi_cases` table. Four locked rules (VOLUME_SPIKE / SUSTAINED_ELEVATION / CROSS_SOURCE_CORRELATION / NEW_TOPIC_EMERGENCE) feed a composite severity scorer with locked weights (indecopi=0.30 / sentiment=0.20 / narrative=0.25 / velocity=0.15 / market=0.10) — sentiment + market are constant proxies, disclosed in composite_breakdown.proxy_channels. Investigation now fires on EITHER system_signal (per-complaint, P-RESHAPE-1) OR pattern (per-bucket, this prompt) via `run_investigation_from_pattern(pattern_id)` with Postgres advisory-lock double-fire prevention. Migration 20260528_0001 adds both tables. Arq cron job runs the tick every 60s. Per-pattern Investigation emits an evidence-only dossier (`trigger_source=PATTERN`); narrative synthesis deferred to Peer Risk Radar (P-RESHAPE-3). | Jorge's team works at pattern level — single-complaint Investigation alone misses the where-the-value-lives signal. See api/sbs_api/aggregation/, tests/integration/test_pattern_to_investigation.py.
2026-05-27 | [P-RESHAPE-3] Peer Risk Radar (PRR) is the 4th agent, chained after pattern-triggered Investigation. New `api/sbs_api/peer_risk/` (cohorts + percentiles + forecast) feeds an on-prem-only LLM narrative call; cloud is hard-rejected in v1 (RuntimeError) and the `peer_risk_analyses.model_provider` check constraint enforces it at storage. Cohort = segment(BANCO/FINANCIERA/CMAC/COOPAC):size_tier(TIER_1/2/3 from tier_classification — portfolio_value column not yet present, documented adaptation). Percentiles/z-score are real numpy over the cohort distribution (raw counts; per-S/.M normalisation deferred until portfolio_value lands). Forecast is hand-rolled Holt-Winters in numpy (not statsmodels — same transparency rationale, avoids scipy/pandas in the API container; locked alpha=0.4/beta=0.2) with PINNED_NULL for non-demo FIs. On LLM-output validation failure PRR falls back to a deterministic template (model_id=prr-template-fallback-v1). Investigation dossier on PATTERN trigger upgrades evidence-only-v1 → narrative-v1, preserving each contributing agent's model_id. Migration 20260528_0002 adds peer_risk_analyses. Conftest seeds 8 synthetic peers (4 BANCO:TIER_1 + 4 COOPAC:TIER_2). | Jorge's most-asked question is "is this normal?" — PRR answers it with peer comparison + forecast. Composite weights from P-RESHAPE-2 are consumed, not re-derived. See api/sbs_api/agents/peer_risk_radar.py, tests/integration/test_pattern_to_peer_risk.py.
2026-05-27 | [P-RESHAPE-4] Issue Resurface is the 5th agent: when PRR flags a sustained HIGH outlier (is_outlier + percentile>=90 + severity HIGH + sustained_days_above_p90>=7) it drafts a pre-escalation FIBrief into AWAITING_APPROVAL (never auto-sends). 30-day per-(institution,motivo) cooldown (hard rule; rejected briefs don't block). Remediation areas are a FIXED enum mapped deterministically from motivo — never LLM-generated. Narrative is on-prem-only (cloud rejected, RuntimeError) bilingual ES-primary/EN-secondary with deterministic template fallback. Peer anonymity is a HARD constraint: peer_context states the FI's own percentile, never names a peer (sentinel tests + an LLM-output guard that rejects any SBS- token). Approval reuses the existing 20-char rationale + head-only gate pattern; only APPROVED briefs deliver. Delivery REUSES the existing signed-webhook infra (sbs_api.webhook.signing + InstitutionWebhookConfig.callback_url as conduct-officer endpoint + OutboundWebhookSecret) — 3 attempts exp-backoff, injectable sender so retry is testable without a live receiver; statuses DRAFT→AWAITING_APPROVAL→APPROVED→PENDING→SENT→DELIVERED/DELIVERY_FAILED→ACKED. Ack endpoint (POST /v1/internal/fi_brief/{id}/ack) captures only — Compliance Tracker consumer deferred to v2. Migration 20260528_0003 adds fi_briefs + fi_brief_audit (append-only). | Closes the loop: gives an FI a chance to self-correct before formal supervisory action. Uses existing auth/HMAC — no reimplementation. See api/sbs_api/agents/issue_resurface.py, api/sbs_api/webhooks/fi_brief_delivery.py, tests/integration/test_pattern_to_fi_brief.py.
2026-05-27 | [P-RESHAPE-5] Persona-scoped cockpit (backend enforcement layer). Five locked SBS-internal personas with fine-grained scopes (sbs_api/auth/persona_scopes.py): conduct_analyst, conduct_supervisor, conduct_head, superintendent (exec aggregates only), sbs_it (ops telemetry only). WB_REVIEWER + FI_USER dropped/absent. Server-side gating via requires_scope/requires_any_scope (403 audited) + two data-filter sentinels: assert_no_business_fields (SBS IT — no business data at all) and assert_no_per_complaint_or_narrative (Superintendent — aggregates + pattern summaries OK, no complaint_id/raw narrative). New endpoint groups: /internal/ops/* (ingestion_lag, agent_health, webhook_health, queue_depth, model_routing[cloud=0 by design], error_tail — all real queries, no mock data), /internal/exec/* (cohort_health w/ data-protection banner, top_patterns_summary, fi_brief_activity_aggregate), /internal/explain/{surface_id} (versioned explanation registry, 15 surfaces, ES+EN+provenance), /internal/persona/* (cross-persona assignment + analyst ack + unit-head FIBrief override w/ 50-char rationale gate — stricter than the 20-char approval gate). New tables persona_assignments + persona_audit (append-only), migration 20260528_0004. Keycloak realm gains sbs:superintendent + sbs:sbs_it roles + sergio/rosa/jorge users. SCOPE NOTE: the five Next.js persona UIs (app/(personas)/*), Explanation.tsx, persona-routing.ts are NOT in this pass — browser-only, cannot verify in this environment; backend enforcement (the testable acceptance criteria) is complete and the BFF can route on primary_persona()/PERSONA_HOME_ROUTE. | Demo feedback: one-size cockpit overwhelmed Jorge + intimidated Sergio. Server-side enforcement is the data-protection story for the Sergio demo moment. See api/sbs_api/auth/persona_scopes.py, api/sbs_api/dependencies/persona.py, tests/auth/, tests/integration/test_persona_demo_state.py.
2026-05-27 | [P-RESHAPE-6] Social media is the third cross-source feed. New api/sbs_api/ingestion/social/: production-shaped SocialAdapter ABC; FixtureSocialAdapter (always used in v1, reads social_signals_fixture); Twitter/Meta stubs that raise NotImplementedError without SBS_TWITTER_API_KEY/SBS_META_API_KEY (so a deploy slip cannot make live calls); entity_resolver (brand-alias lookup + NER stand-in); runner (anonymize via reused redaction engine + /@\w+/→[HANDLE], resolve, classify fraud indicators, persist idempotent on (source,source_post_id)). Fifth pattern type FRAUD_EMERGENCE (per-institution, fuses 72h social + 24h fraud complaints + 7d INDECOPI) scored with a SECOND locked weight set (social=0.30/complaints=0.25/indecopi=0.20/velocity=0.15/market=0.10) — distinct from RESHAPE-2's standard weights which are unchanged for the other 4 types. HIGH FRAUD_EMERGENCE → Investigation → PRR → Sector Broadcast (new agent): warns same-cohort peers, origin FI NEVER named, threat narrative anonymized on-prem w/ template fallback, fixed suggested-controls enum. Sector broadcast carries DUAL approval (primary: supervisor/head; secondary: head OR superintendent — explicit Sergio read-only exception, see docs/personas/superintendent.md), 50-char rationale each, distinct approvers; delivery reuses the signed-webhook chain with X-Payload-Type: SECTOR_BROADCAST header + per-recipient idempotency. Migration 20260528_0005 (6 tables + extends pattern_type check constraint to add FRAUD_EMERGENCE). New ops/social_health + exec/sector_broadcasts panels. Route-scope ratchet: scripts/audit_route_scopes.py + tests/auth/test_all_routes_scoped.py asserts every v1 route is scope-protected or in the PUBLIC_ROUTES allowlist (59 routes, 0 unscoped). arq crons: aggregation tick 60s, social ingestion 5min. | Jorge's flagship: detect fraud campaigns in real time by fusing complaints+social+INDECOPI and warn the sector before it's hit. See api/sbs_api/agents/sector_broadcast.py, tests/integration/test_fraud_emergence_to_sector_broadcast.py.
2026-05-28 | [P-RESHAPE-7] Insight chatbot backend — the supervisors' daily-use surface. agent_id=insight-chatbot, on-prem only (cloud rejected RuntimeError, same as PRR/Issue-Resurface/Sector-Broadcast). Function-calling over 6 LOCKED, parameter-validated tools (query_complaints / query_patterns / query_fi_profile / query_cross_source / chart_it + IT-only ops_query) — NO free-form SQL, NO write tool ever (enforced at the dispatcher). Defense against hallucination is tight parameters, not prompting. Persona scope enforced at the tool dispatcher BEFORE the LLM sees the inventory (api/sbs_api/agents/insight_chatbot_scope.py): Analyst+UnitHead get query_complaints; Supervisor/Superintendent do not (no complaints:read:detail) so Sergio can never reach a per-complaint row; Superintendent is aggregate_only; SBS IT gets ONLY ops_query (no business tools). Provenance mandatory: every entity-referencing answer must carry >=1 citation {tool, parameters, row_ids_returned, query_hash}; an uncited entity answer is a validation failure → deterministic template fallback (model_id=chatbot-template-fallback-v1). Per-turn tool-call cap 5; per-session 50 msgs + 1h idle; per-user 60/h + 200/day — structured 429, never silent (insight_chatbot_limits.py). User input anonymized via the existing redaction engine BEFORE storage AND before the LLM. New chatbot:use scope granted to all 5 personas (no FI grant — FI users don't exist). Migration 20260528_0006 (chatbot_sessions + chatbot_messages, audit-retained). 4 routes under /v1/chatbot, all scope-gated; route-scope ratchet stays green (63 routes, 0 unscoped). seed_demo.py seeds a sample Jorge session grounded in the real fraud pattern. | Useful on real questions, not impressive on synthetic ones. UI lands in P-RESHAPE-10. See api/sbs_api/agents/insight_chatbot.py, tests/integration/test_chatbot_end_to_end.py.
2026-05-28 | [P-RESHAPE-9] Cleanup + contract bump v0.2.0 + IT remediation + chatbot polish (closeout before RESHAPE-10 frontend). (1) Removed the two ReplayProvider SCAFFOLD agents (taxonomy_harmonizer, cross_source_correlator) + their replay fixtures + orchestrator routing (include_scaffolded param + OrchestrationResult.cross_source dropped); ReplayProvider class stays (testing primitive); the real triage/investigation/synthesis agents are unchanged. The cross-source cockpit strip + anomaly card still render from seeded/historical agent_runs (readers key off the agent_name string, not the deleted module). AGENT_REGISTRY stays at exactly 6. (2) Contract bump 0.1.0→0.2.0: new SupervisoryMetadata block (system_signal / system_signal_reasons / validation_verdict / triage_classified_at) added to ComplaintListItem (RESPONSE-side only) on BOTH the canonical OpenAPI yaml and the Pydantic model (JSON schemas regenerated). FI submission contract (Complaint) UNCHANGED — extra:forbid + locked Annex 1-A fields, SupervisoryMetadata absent (asserted both ways). build-standards-pack.sh PACK_VERSION→0.2.0, pack openapi → sbs-complaints-v0.2.yaml; CHANGELOGs added (docs/schemas + standards-pack). Portal version pill → v0.2.0. (3) IT remediation for Rosa (new ops:remediate scope, SBS IT only): POST /internal/ops/webhooks/{id}/retry (delivery_failed→pending, attempts++), /runs/{id}/requeue (failed agent_run → fresh in_progress row), /circuit_breaker/{institution_code} (PAUSE/RESUME, 50-char rationale). New fi_circuit_breakers table (migration 20260528_0009); ingestion (POST /v1/complaints + pipeline.run_tier1_ingestion) rejects a PAUSED FI with 503 fi_circuit_breaker_paused (new CircuitBreakerPaused exception, code SBS-503-003). Every remediation lands an incident_annotations audit row. Conduct personas → 403. (4) Insight Chatbot persona-suggested questions: GET /v1/chatbot/suggested_questions returns a workflow-tuned 3-question list per persona (IT = ops-oriented, Conduct/exec = business). Route ratchet green (88 routes, 0 unscoped); PII sentinels green; +30 tests (937 total). | Last consolidation before the frontend pass — folds in deferrals from RESHAPE-1..8.5. v0.2.0 is the canonical contract for the July transition; the FI wire contract is untouched. See api/sbs_api/routes/ops_remediation.py, api/sbs_api/db/models/fi_circuit_breaker.py, api/sbs_api/agents/insight_chatbot_suggestions.py, tests/ops/, tests/standards_pack/, tests/cleanup/.
2026-05-28 | [P-RESHAPE-8.5] Unified agent monitoring + persona action surface + task inbox/outbox. (A) New LOCKED-in-code agent registry (api/sbs_api/agents/registry.py) over 6 agents — 3 FI-facing character cards (DIValeVale/Reclamito/Lupaman) + 3 internal (Triage/Investigation/Insight-Chatbot). Lupaman is a UX-ONLY composite of peer-risk-radar + sector-broadcast: the two stay separate in code/DB/audit, telemetry merged only at read time, each recent_run labelled with its underlying agent_id (provenance preserved). New GET /v1/internal/cockpit/agents (list, persona-shaped: Conduct=6 full, Superintendent=3 FI-facing aggregate-only via new agents:read:exec scope + recent_runs dropped, SBS IT=6 with display branding stripped), /{id}/runs, /{id}/runs/{audit_id} (Superintendent 403, IT stripped to timing+status+model_id, Conduct full), and /stream (SSE over the existing bus). Status (RUNNING<5min in_progress / DEGRADED success<0.80 or p95>5000ms / IDLE) computed at query time from agent_runs — no status table (api/sbs_api/agents/status.py). Existing DIValeVale validation_audit router kept and registered BEFORE the unified router so /agents/divalevale/runs/{id} keeps its richer detail; other ids fall through to agent_runs. (B) Action surface: LOCKED action registry (api/sbs_api/personas/action_registry.py) of per-persona verbs; GET /v1/internal/cockpit/actions returns the caller's set. New minimal endpoints (scope + rationale-length + persist + 201, no state machines): findings/manual (propose_pattern→manual_findings), complaints/{id}/flag + /request_enrichment, findings/{id}/delegate (→persona_tasks PATTERN_DELEGATION) + /defer, exec/tasking (→persona_tasks DEEPER_LOOK), exec/digest/generate + /{id}/acknowledge (→digest_audit), ops/incidents (→incident_annotations, business-field-free). Approvals/overrides/sector-broadcast co-approval point at existing routes (NOT reimplemented). New scopes: agents:read:exec, findings:propose, complaint:flag, complaint:request_enrichment, pattern:delegate, pattern:defer, digest:generate, digest:acknowledge, exec:tasking, incident:annotate. "Any authenticated persona" gate = requires_any_scope(*ALL_SCOPES). (C) Task inbox/outbox on new persona_tasks table (OPEN→ACKED→COMPLETED/DECLINED; decline needs 30-char rationale); inbox returns user-targeted OR persona-targeted; ack/complete/decline callable only by the assigned persona or named assignee. persona_tasks is authoritative for the action-surface lifecycle; the older persona_assignments (P-RESHAPE-5) stays for its ack-only handoff, not joined. Migration 20260528_0008 (persona_tasks + manual_findings + digest_audit + incident_annotations). seed_demo seeds agent_runs across all 6 agents (1 in_progress) + 2 sample tasks. Route ratchet green (84 routes, 0 unscoped); +40 tests (907 total). | Demo feedback: every persona's dashboard must FEEL built for them, not just filtered — no persona stares at read-only data. Feeds the RESHAPE-10 frontend. See api/sbs_api/agents/registry.py, api/sbs_api/personas/action_registry.py, tests/cockpit/test_agents_*.py, tests/integration/test_persona_action_flow.py.
2026-05-28 | [P-RESHAPE-8.6] Persona rename + dev auth stub. Renamed the two demo personas whose placeholder names collided with real SBS staff: mariela→jorge (Conduct Unit Head, "Jorge Caballero", jorge@sbs.gob.pe) and patricia→rosa (SBS IT, "Rosa Salazar", rosa@sbs.gob.pe); Lucía/María/Sergio unchanged. Rename is string-only across active surfaces (code/tests/realm JSON/seed/DECISIONS/DEMO) — role/scope identifiers (sbs:conduct:head, sbs:sbs_it) untouched. Historical point-in-time docs (ADRs, session journals, PLAN, research, explainer text) intentionally NOT rewritten: those record "Mariela (supervisor)" as it was on its date; rewriting dated records would falsify them. New dev-only auth stub (api/sbs_api/dependencies/auth_stub.py): `Authorization: Bearer stub:<persona>` resolves to that persona's role + exact persona-scope set, GATED on BOTH SBS_API_AUTH_STUB_ENABLED=true AND SBS_API_ENVIRONMENT=dev — ignored (real JWT/Keycloak path runs) in any other config; never honoured in staging/prod. Every resolution logs WARNING; unknown persona → 401; startup INFO line shows accepted/rejected. Wired inert-unless-dev+flag into verify_internal_secret, requires_scope/requires_any_scope, verified_mtls_subject, and the OAuth scope dependency (so stub:sergio → /v1/complaints returns a real 403 scope denial, not a stub-blind 200). | Local demo rehearsal needs persona scoping without standing up Keycloak; and "Mariela"/"Patricia" must not appear as demo usernames for real people. See api/sbs_api/dependencies/auth_stub.py, tests/auth/test_stub_auth.py, docs/personas/README.md, scripts/demo-curl-examples.sh.
2026-05-28 | [P-RESHAPE-8] DIValeVale — pre-ingestion data-quality validation agent, sits BEFORE Triage (ingestion/pipeline.py:run_tier1_ingestion records the stage trace divalevale→triage). One of three FI-facing character-named agents (DIValeVale/Reclamito/Lupaman). Pass 1 deterministic schema+completeness (no LLM) → verdict VALID/RECOVERABLE/INSUFFICIENT/INVALID with enum failed-rule codes; institution_code regex ^[A-Z]{3}_[A-Z]+_\d{3}$, amount required for fraud-class motivos. Pass 2 (RECOVERABLE only) regex extraction (single amount → propose-fill; date) + a SINGLE sparing on-prem LLM call ONLY when regex finds multiple amount candidates (conf>=0.85 fills, else FLAGGED_FOR_REVIEW); malformed LLM output flags, never crashes/blocks; PII redacted before the LLM. Tier-differentiated routing: Tier-2 batch quarantines on >=1 INVALID OR >=20% INSUFFICIENT → diagnostic report + VALIDATION_BATCH_REJECTION webhook, no rows to Triage, idempotent resubmit on batch_id (in-place replace); Tier-1 INSUFFICIENT → enrichment_request + VALIDATION_ENRICHMENT_REQUEST webhook (5 biz days), record preserved, one enrichment cycle then ESCALATED; INVALID → reject. On-prem only (cloud RuntimeError). Both webhooks reuse the existing HMAC chain, carry codes+ids only (no narrative). Migration 20260528_0007 (validation_audit + validation_batches + enrichment_requests). New agents:read scope (Conduct + IT; NOT Superintendent) gates cockpit /internal/cockpit/agents/divalevale/{activity,runs/{id}} — IT telemetry-only, Analyst scoped to assigned FIs, Unit Head full detail. Route ratchet green (65 routes, 0 unscoped). | FI data is often misshapen (amount in narrative, fields too short); current pipeline silently dropped bad rows. DIValeVale recovers or routes back to the FI — no silent loss. See api/sbs_api/agents/divalevale/, tests/integration/test_divalevale_full_chain.py.

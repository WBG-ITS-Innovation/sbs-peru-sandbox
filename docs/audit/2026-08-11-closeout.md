# Closeout — the two final-check findings

- **Date:** 2026-08-11
- **Branch:** `fix/handover-p0`, now **23 commits**. Nothing pushed.
- **Input:** [2026-08-11-final-check.md](2026-08-11-final-check.md) §Findings
  (F1, F2). Both closed. Docs only — no component, script or config was
  touched.
- **Ground rules held:** additive, hooks passed on both commits (`--no-verify`
  not used), no push, no migration, README steps 1–3 not touched.

---

## C1 — `app/README.md` route classifications (F1)

**Commit `a2865bf`** — `docs(app): correct five route-map cells that disagree
with the components`.

F1 named two wrong cells. Sweeping the rest of the map found three more, one of
them in the opposite direction. All five were verified by following the imports
out of each `page.tsx` into every component it renders, treating a route as
live only if something on that path issues an `internalGet`, `fetch`, or SSE
subscription.

### Fixed — claimed live, makes no data call

| Route | Was | Now | Evidence |
|---|---|---|---|
| `/app/analytics` | Live — client fetch | Static — pilot preview: every figure is a hard-coded constant, all controls disabled, no backend call | 382-line server component. Zero `fetch(` / `useEffect` / `'use client'` / `internalGet`. `MATRIX_ROWS`, `TOP_MOTIVOS`, `TOP_PRODUCTS`, `kpis` (`'384'`, `'91.4%'`, `'98%'`) and `cadence` are all literals in the source; all three export buttons carry `disabled aria-disabled="true"`. Its own header: *"No backend calls are made from this page; every value is illustrative."* |
| `/app/queue` | Live — client fetch | Static — stub: renders an `EmptyState` pointing at Findings, no data call | 28 lines total. Header: *"Risk Queue — stubbed for the May 25 demo per the drop ladder."* |
| `/app/fi/banco-demo-001/inbox` | Mixed — live fetches plus the golden complaint fixture | Static — renders `@/lib/journey-emails.json` with the golden complaint badged; no data call | 19-line server component handing two fixtures to `FIInbox`; `FIInbox.tsx` (154 lines, `'use client'`) contains no `fetch`, `useSWR` or `EventSource`. The golden complaint is a *pointer used to badge a row*, not a fetch. |
| `/app/fi/banco-demo-001/triage/[rowIndex]` | Live | Static — renders one `journey-emails.json` row; no data call | Page indexes the fixture array and passes one row to `FITriage.tsx` (200 lines), which makes no data call. |

### Fixed — claimed static, is live

| Route | Was | Now | Evidence |
|---|---|---|---|
| `/app/ingestion` | Static — renders `@/lib/journey-emails.json` | Live — ingestion theatre: POSTs `@/lib/journey-emails.json` rows through `/app/api/journey/submit`, polls `/app/api/journey/recent` | `IngestionClient.tsx:125` POSTs `/app/api/journey/submit`; `:177` polls `/app/api/journey/recent?limit=20` every 2 s. The JSON is the *send corpus*, not the rendered content. The page header calls it "live ingestion theatre". |

This one runs the other way from F1 and was outside its brief, but it is the
same defect in the same table — a reader avoiding `/app/ingestion` because the
map calls it static would skip the screen that drives the real signed pipeline.
Fixed and listed rather than left.

### Checked and correct — the other nineteen cells

| Route | Claim | Data call actually reached |
|---|---|---|
| `/app/cockpit` | Live — server-rendered via `internalGet` | `page.tsx` → `/v1/internal/cockpit`, `/v1/internal/cockpit/taxonomy-stats`; `InsightsBoard` → `/app/api/journey/insights`; `hooks/useSSE.ts` |
| `/app/cockpit/aggregates` | Live — client fetches `/app/api/aggregates/*` | `AggregateTables`, `RedFlags`, `LiveIngestionBanner`, `AnalysisAssistant`, `AggregatesDocs` → `trend`, `patterns`, `sources`, `social`, `feed`, `chat` |
| `/app/findings`, `/app/findings/[id]` | Live — server-rendered | `lib/api.ts` server calls; `[id]` also `DraftNarrativeEditor`, `ActionsRow` |
| `/app/approvals`, `/app/approvals/[id]` | Live — server-rendered | `lib/api.ts`; `[id]` also `DecisionActionsRow` |
| `/app/audit` | Live — server-rendered | `lib/api.ts` |
| `/app/processing`, `/app/processing/[id]` | Live — client fetch | `ProcessingListClient` → `/app/api/journey/recent`; `ProcessingDrilldown` → `journey/audit`, `journey/findings`, `complaints/[id]` |
| `/app/assistant` | Live — client fetch | `LiveAssistantChat` → `/app/api/demo/assistant` |
| `/app/admin` | Live — client fetch via `/app/api/admin/audit` | `SandboxAdmin` → `/app/api/admin/audit`, `/app/api/aggregates/feed` |
| `/app/sandbox` | Live — client fetch, signed sends | `SandboxControl` → 4 × `/app/api/sandbox/*`; `LiveIngestionBanner` |
| `/app/demo-journey` | Mixed — live plus `golden-complaint.json` | `DemoJourneySbs` → `/app/api/journey/audit`, `/app/api/journey/findings` |
| `/app/fi/.../send` ×2 | Live — real signed sends | `SandboxControl` → `/app/api/sandbox/*` |
| `/app/fi/.../submit/[rowIndex]` | Live | `FISubmit.tsx:78` → `/app/api/journey/submit` |
| `/app/docs`, `/app/rr1` | Static | none reached |
| `/app/`, `/app/login` | redirect / Keycloak entry | none reached |
| `/app/developers`, `/app/developers/credentials`, `/app/_components` | Static | none reached |

24 page routes classified; 19 already correct, 5 corrected. The commit also
adds a short paragraph defining what "Live" and "Static" mean here and telling
the next reader to re-derive a cell by following imports rather than by looking
at the screen — the failure mode being exactly `/app/analytics`, a static page
full of plausible figures.

---

## C2 — the documented cockpit path produces agent runs (F2)

**Commit `ab0a2a4`** — `docs(readme): give the cockpit section's :8443 command
the agent pipeline`.

The cockpit section's step 2 read *"Institution-facing API on :8443 (README
step 2 above)"*. That command sets no `SBS_API_AGENTS_PIPELINE_ENABLED`, which
defaults to `False` (`api/sbs_api/config.py:318`), so the section as written
produced a `:8443` process that ingested complaints and wrote **zero**
`agent_runs`.

The command is now spelled out in the section rather than cross-referenced, so
the two invocations cannot drift, plus one sentence on the default and the
worker-side equivalent:

```diff
-# 2. Institution-facing API on :8443 (README step 2 above).
+# 2. Institution-facing API on :8443 — step 2 above, plus the agent
+#    pipeline, so submitted complaints get an agent chain to show.
+SBS_API_MTLS_MODE=direct \
+SBS_API_AUTH_STUB_ENABLED=false \
+SBS_API_PORT=8443 \
+SBS_API_AGENTS_PIPELINE_ENABLED=true \
+  bash scripts/run-api.sh
```

> `SBS_API_AGENTS_PIPELINE_ENABLED` defaults to **off**, so a `:8443` process
> started without it ingests complaints normally but writes no `agent_runs` —
> the cockpit renders the complaint with an empty agent chain. The Tier-2
> equivalent is a separate flag on the worker container; see
> `docs/HANDOVER-NOTES.md` under "Operating the stack".

**README steps 1–3 were not touched.** Both diff hunks land at lines 60 and 82,
inside the cockpit section; the three-command quickstart at lines 15–29 is
byte-identical, so B2's "run literally" verification still refers to the same
text.

### The walk — amended section only, from both API processes dead

```
$ 8443=0  8000=0                                       (both killed first)

########## step 1 ##########
$ bash scripts/dev-up.sh                               exit=0
$ docker compose up -d keycloak                        Container sbs-keycloak Running

########## step 2 — the NEW command, verbatim ##########
$ SBS_API_MTLS_MODE=direct SBS_API_AUTH_STUB_ENABLED=false \
  SBS_API_PORT=8443 SBS_API_AGENTS_PIPELINE_ENABLED=true \
    bash scripts/run-api.sh
INFO:     Uvicorn running on https://0.0.0.0:8443        pid=60568

########## step 3 ##########
$ SBS_API_PORT=8000 bash scripts/run-api.sh
INFO:     Uvicorn running on http://0.0.0.0:8000         pid=60569

########## one signed Tier-1 complaint ##########
  BCO-2026-773001

       agent_name        | status  | model_provider | n_tools |          label          |   route_to
-------------------------+---------+----------------+---------+-------------------------+---------------
 triage                  | success | mock           |       3 | undisclosed-fees-credit | investigation
 investigation           | success | mock           |       4 |                         |
 synthesis               | success | mock           |       2 |                         |
 cross-source-correlator | success | replay         |       2 |                         |
(4 rows)

########## cockpit ##########
$ curl .../app/api/auth/demo-login    307 → http://0.0.0.0:3000/app/cockpit
$ curl .../app/cockpit                http=200  bytes=89072
  grep -c BCO-2026-773001             1
```

The four-row chain with `n_tools > 0` on every agent is exactly what the
section previously could not produce: the same walk against the old text gave
`(0 rows)` (final-check §3). The cockpit renders the complaint submitted
seconds earlier.

### Nothing regressed

```
stage-h-contract   exit=0   37 passed in 3.37s
stage-h-full       exit=0   37 passed in 3.12s   live half: PASS
```

```
[14:37:37] tier1: agent_runs triage(status=success,provider=mock,tools=3), investigation(...,tools=4), synthesis(...,tools=2), cross-source-correlator(provider=replay,tools=2)
[14:37:38] tier1: agent_runs triage(status=success,provider=mock,tools=3), investigation(...,tools=4), synthesis(...,tools=2), cross-source-correlator(provider=replay,tools=2)
[14:37:38] --- Tier 2: signed POST /v1/batches, 2 rows (worker container) ---
[14:37:40] batch complete: accepted=2
[14:37:40] tier2: agent_runs triage(status=success,provider=mock,tools=3), ... (row 1/2)
[14:37:40] tier2: agent_runs triage(status=success,provider=mock,tools=3), ... (row 2/2)
[14:37:40] stage-h-full live assertion: PASS

  contract half: PASS
  live half:     PASS
stage-h-full: PASS
```

---

## Branch

```
$ git log --oneline oss-release-pr..HEAD | wc -l
23

ab0a2a4 docs(readme): give the cockpit section's :8443 command the agent pipeline
a2865bf docs(app): correct five route-map cells that disagree with the components
55618b0 fix(compose): probe the realm Keycloak actually serves in its healthcheck
6f9f03a docs(app): replace the stale route map with the one on disk
9874933 fix(app): resolve the shelled-out Python interpreter from an env var
7880de8 docs(adr): record ADR 0001's roster as built, in an addendum
bab670e docs(handover): write down the sharp edges and the audit-trail errata
c6a8c61 docs: document the cockpit bootstrap, both API processes included
7cbb429 docs(readme): give step 2 the port step 3 already requires
c7910c2 fix(agents): key the mock provider's script cursor per complaint
107d56f docs(agents): describe run_agent_pipeline_on_new as the backfill utility it now is
7cb6d84 test(db): guard the inverse direction — no table-level autogenerate drift
f5beb8c fix(db): add ORM models for the four tables that had none
d6d06dc fix(ui): Anexo 1-A tab renders 27 fields under its "27 campos" heading
8290474 fix(agents): key classify_complaint on the enums the API actually accepts
ea95251 docs(openapi): document POST /v1/sandbox/complaints/granular in the contract
9a935cd fix(scripts): .env supplies defaults to run-api.sh, not overrides
bd5daef fix(tests): implement the stage-h gate pair; stop stage-g-full skipping its live half
5fd605d fix(agents): persist which provider actually served each agent run
9b2dc4e fix(agents): run DIValeVale ahead of Triage on the ingest path
ed2e812 fix(agents): run the Part-12 chain on both canonical ingestion tiers
4fab390 fix(tests): make the Spectral gate step parse Spectral's JSON output
2e8963f fix(db): register the 9 ORM models missing from the metadata graph

$ git status --short
?? docs/audit/
```

Two commits, one per item, both docs-only, both through the hooks. The working
tree is otherwise unchanged.

## Final gate pair

```
stage-h-contract   exit=0   37 passed in 3.37s
stage-h-full       exit=0   37 passed in 3.12s   live half: PASS
```

## Session state

- **Nothing pushed.** Two new commits on `fix/handover-p0`; head `ab0a2a4`.
- Running: `:8443` pid 60568 (mTLS, **agent pipeline on** — started per the
  amended section), `:8000` pid 60569, `next dev` on `:3000` untouched. All five
  compose services up, keycloak healthy. Worker still has
  `SBS_API_AGENTS_PIPELINE_ENABLED=true` and `SBS_API_MODEL_PROVIDER=mock`.
- No migration, no `docker compose down`, no `git stash`, no tracked file
  modified beyond the two committed documents.

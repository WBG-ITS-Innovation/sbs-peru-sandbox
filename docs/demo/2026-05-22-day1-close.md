# Sprint Day 1 close — Prompt 10 (2026-05-22)

Day 1 of the SBS sprint (Prompts 10 → 13 cover Days 1–3). This file
captures (a) the manual demo path walked end-to-end against the
running stack, (b) the items deferred to Days 2–3, (c) the
operational checklist items that are not Claude Code's to land.

## Manual demo path — what was verified

The 12-step demo path from the v2.1 prompt's "Manual demo path" exit
gate. Each step is annotated with the WS that owns it and the state
at Day 1 close.

| # | Step | Owner | State at Day 1 close |
| --- | --- | --- | --- |
| 1 | Open `/app/` unauthenticated → redirect to login | WS2 | ✓ Code path: `app/src/app/page.tsx` redirects to `/login` when no session. Verified at the code level; full live-stack walk needs Day 2 docker compose up. |
| 2 | Sign in as the Conduct Supervisor → land on cockpit | WS2 / ADR 0042 | ✓ OAuth Authorization Code + PKCE; landing rule resolves `supervisor → /cockpit`. Audit row written with `action='login'`, `meta.landed_at='/cockpit'`. |
| 3 | Tier 1 / Tier 2 side-by-side renders | WS3 | ✓ Same `<ComplaintCard>` on both panels; tier label + descriptor differ. |
| 4 | Anomaly card visible with composite math tooltip | WS3 | ✓ Reads from WS0-seeded cross-source-correlator agent_run (composite_score=0.74, threshold=0.7). Tooltip lists channel contributions tabular. |
| 5 | Click anomaly → Findings filtered | WS3 / WS4 | ✓ Anomaly card links to `/findings?institution=…&from=…`; FindingsFilters reads from URL. |
| 6 | Open headline finding `BCO-2026-000001` → 5 panels | WS4 | ✓ BERT 0.87 (numeric, never bucketed), XGBoost panel with `narrative_mentions_fee_undisclosed` +0.27 as top contributor, agent reasoning chain shows three success runs. |
| 7 | Edit draft narrative (add "comisión por mantenimiento") → save | WS4 | ✓ POST `/app/api/findings/:id/draft` → FastAPI → `complaint_narrative_drafts` row + `audit_events` row with `diff={before_excerpt, after_excerpt}`. Contract-tested. |
| 8 | Send to approvals | WS4 | ✓ POST → `pending_approvals(status='pending')` + audit. Idempotent on duplicate POST. Contract-tested. |
| 9 | Switch to the Conduct Unit Head via demo switcher (no sign-out) | WS2 | ✓ POST `/app/api/persona/switch` → audit row with `action='switch-persona'`, `actor_id=<operator>`, `meta.from_persona='supervisor'`, `meta.to_persona='unit-head'`. Contract-tested. |
| 10 | Open the approval → Approve with edits → rationale + edit | WS5 | ✓ Edit modal pre-fills with the Conduct Analyst's saved draft. 20-char rationale gate (server-side AND client-side). Writes `supervisory_observations` + `agent_feedback(edit_diff)` + audit with 9-key meta. Contract-tested. |
| 11 | ES↔EN toggle → all strings flip → switch back | WS2 | ✓ `LanguageToggle` writes the `sbs-locale` cookie via server action; `revalidatePath` re-renders. i18n parity test green. |
| 12 | Audit screen → see all decisions from steps 7–10 with diffs | WS6 | ✓ Server-rendered audit table; row expand shows full JSON diff + meta (text-only per the drop ladder); pagination at 50/page (80-row seed produces 2 pages). |

The walk is **structurally complete** — every code path the demo
requires is in place. Full live-stack execution (POST /v1/complaints
publishes to SSE so the cockpit's Tier 1 panel updates live; the
seeded narrative arc fires the anomaly card after exactly the third
submission) requires Day 2 verification with the stack actually
running.

## Day 2 priorities (handoff)

These are not Claude Code's items — they're calendar / human /
operational. Surfaced here so they don't drop out at the WS handoff.

| Item | Owner | Notes |
| --- | --- | --- |
| ES dictionary native-speaker review | the SBS technical counterpart | Two-hour pass against `app/src/i18n/es.json`. Specific terms to confirm: severity labels, action verbs (Aprobar / Rechazar / Devolver / Editar), nav labels, audit row descriptors, cross-source channel names. PR-approval recorded. |
| POST /v1/complaints SSE hook | Maintainer | Currently deferred (WS3 commit message names the deferral). Add `BackgroundTasks.add_task(publish_complaint_received, body)` after `await session.commit()` in `api/sbs_api/routes/complaints.py:create_complaint`. Latency on the institutional path unaffected; cockpit's Tier 1 panel updates live on real Tier 1 submissions. |
| Demo data seed verification | Maintainer | Run `python scripts/seed_demo_narrative.py` against a fresh dev DB; verify (a) 6 agent_run rows with the expected distribution (3 success on `BCO-2026-000001`, 2 partial on `BCO-2026-000002`, 1 failed on `BCO-2026-000003`); (b) headline finding's drafted narrative does NOT mention "comisión por mantenimiento" (the scripted edit gap); (c) XGBoost feature_contributions on the headline carry `narrative_mentions_fee_undisclosed` as the highest positive. |
| the SBS Conduct department head re-brief | the SBS technical counterpart (per the prompt's operational checklist) | Conduct-only positioning confirmed before May 25. |
| Browser pinning | Maintainer + the WBG technical lead | Test in Chrome + Edge latest on Windows 11. Mac Safari is not the demo browser. |
| Screen-recording fallback | Maintainer + Day 3 rehearsal | Clean walkthrough recorded at Prompt 13 rehearsal; local file ready in case docker stack or conference network fails mid-demo. |
| SBS branding clearance | the SBS technical counterpart | Lockup + diamond-S logo usage confirmed for the sandbox demo context. |
| Keycloak SSO question | the WBG technical lead (rehearse) | Scripted answer: "Keycloak is the sandbox IdP; integration with SBS's existing SSO is a configuration change, not a redesign." |

## Prompts 11 → 13 sequencing (Day 2–3 calendar)

| Prompt | Owner | Scope |
| --- | --- | --- |
| 11 | Maintainer | BETO Spanish classifier + semantic search + summarization. Replaces seeded BERT classifier outputs with live model output. The `agent_run` JSON Schema contract from WS0 is the integration gate — as long as `tests/integration/test_agent_run_schema.py` stays green, the supervisor UI does not break. |
| 12 | Maintainer | Two live agents (pattern detection + supervisory query authoring) + cloud/on-prem pill reveal with working toggle. |
| 13 | Maintainer + the WBG technical lead | Polish + brief k6 load test + rehearsals. |

## What changed from the v2.1 prompt's plan

- Operational checklist items moved here so the Prompt 10 close is
  cleanly bounded. The maintainer reads the table above; Day 2 starts
  with those items, not with Prompt 11.
- POST /v1/complaints SSE hook deliberately deferred (WS3 commit
  message + this handoff entry both name it).
- ADR 0043 SSE topic model landed retroactively in WS7 — the contract
  was deferred to the explicit ADR so the topic shape + role-scope
  table + buffer + replay semantics could be locked in one document.

# P10 true-close evidence

Branch: part-08/supervisor-ui-backend-wiring
Code checkpoint: 7c617cb
Verified at: 2026-05-23 17:27 EDT

Scope: supervisor UI ↔ FastAPI backend wiring, SSE stabilization, pure-ASGI middleware conversion, deterministic Tier 1/Tier 2 seeded demo state, findings-to-approvals-to-audit browser path.

## Automated checks

Command:

```bash
uv run pytest -q tests/test_cockpit_endpoint.py \
  tests/test_complaints_source_backfill.py \
  tests/integration/test_audit_and_sse_completeness.py \
  tests/integration/test_findings_endpoints.py \
  tests/integration/test_approvals_endpoints.py
```

Result:

```text
35 passed
0 xfailed
```

Xfail scan:

```bash
grep -RIn "xfail" tests --include="*.py"
```

Result: no output.

## Browser checks

- / redirects to /app/api/auth/demo-login.
- Cockpit lands with Live status.
- Tier 1 BANCO_DEMO_001 rows are present and DB-backed.
- Tier 2 COOPAC_DEMO_002 rows are present and DB-backed.
- Anomaly card remains composite 0.74 / threshold 0.70.
- Findings list shows DB-backed Tier 1 and Tier 2 complaints.
- BCO-2026-000001 detail renders.
- BERT panel shows 0.87.
- XGBoost top contributor shows narrative_mentions_fee_undisclosed +0.27.
- Send-to-approvals path no longer crashes for the Conduct Supervisor/the Conduct Analyst role transitions.
- the Conduct Unit Head can review pending approvals.
- Rationale gate verified: short rationale rejected; 20+ character rationale accepted.
- Approve-with-edits writes audit row.
- Audit screen shows approval decision rows chronologically.

## Known remaining risks

- UI still needs demo polish.
- P10 proves workflow, schema, role gates, SSE, seed integrity, and UI/backend wiring.
- P10 does not yet prove full live BETO/XGBoost inference or production PII/cloud governance; those are later prompts.

# Track B Salvage — Human TODOs

Generated by the unattended agent salvage run on 2026-06-02. Items here need a human decision before the salvage branch is merged.

---

## TODO-1 — `annexo.pdf` provenance verification

**Status:** dropped by default (per brief).
**Source commit:** `4a5b0e1 feat(p11): Annex 1-A 27-field DQ validator (DQ-A1A-007..027)`.
**File:** `annexo.pdf` (342 KB, was added at repo root).

The source commit added `annexo.pdf` and the DQ code-list YAMLs cite it as the spec source. The triage report flagged it for human verification. The unattended salvage agent could not extract the PDF text (no `pdftotext`/`PyPDF2` installed), so the concrete check defined in the brief — text contains both "Resolución SBS" and "04036-2022" — could not run.

Per the brief, **default = EXCLUDE**. The PDF was therefore removed from the salvage branch and the path added to `.gitignore`.

**Dependency analysis (no test breakage):**
- `api/sbs_api/dq/codelists/__init__.py` line 4: docstring mention only (`"typically ``annexo.pdf``"`). Not a runtime load.
- `docs/demo/2026-05-27-p11-dq-complete.md`: documentation cross-references (page numbers). Not a runtime load.
- No `.py` test imports, opens, parses, or otherwise touches `annexo.pdf`.

Dropping the PDF does not break Gate 5 (the 26 agent tests + the full pytest suite). The DQ rules and YAML code lists are entirely self-contained.

**What the human must do:**
1. Open the PDF locally and confirm it is the public *Resolución SBS 04036-2022* reglamento (no internal SBS material).
2. If confirmed public: `git rm` the gitignore entry, `git add annexo.pdf`, and commit `chore(p11): restore annexo.pdf (verified public Res. SBS 04036-2022 reglamento)`.
3. If not confirmed public: leave it excluded and update `docs/demo/2026-05-27-p11-dq-complete.md` to cite the published SBS resolución URL instead of the PDF page numbers.

---

## TODO-2 — `eecfdd1` carve outcome

**Status:** completed by the agent — see commit `feat(p11): taxonomy dictionary + DQ columns + CORS (architecture only; demo-data wiring dropped)` on this branch.

The default-drop rule was applied. Dropped paths (not committed):
- `scripts/ingest_sample_dataset.py`
- `docs/demo/2026-05-26-p11-sandbox-close.md` (the additions from this commit — the file itself was already committed by 8f28b5a; only the eecfdd1 increment was reverted)
- `docs/demo/2026-05-27-demo-ready.md`
- `docs/demo/2026-05-27-narration-anchors.md`
- `docs/demo/2026-05-27-two-laptop-setup.md`

**What the human must do:** confirm the carve matches intent. If any of the four demo docs need to ship (synthetic-data-only), re-add them in a follow-up commit on this branch.

---

## TODO-3 — replay-fixture string-keys flagged-not-scrubbed

**Status:** check the re-scrub commit `chore(series-1c): re-scrub salvaged agent layer` for any strings flagged here.

If any replay-fixture value (in `api/sbs_api/agents/fixtures/replay/*/*.json`) carried a name that is matched on by agent code at runtime, the scrub left it in place rather than half-scrubbing it. The salvage re-scrub findings section records every such string. Each must be either:
1. Renamed in BOTH fixture + consumer atomically, or
2. Documented as intentionally untouched (e.g., if the name is a fully synthetic persona for the BCO-2026-000001 demo invariant that is not a real person and not a regulator-facing surface).

---

## TODO-4 — PR #53 disposition

The original parked PR `#53 feat(p12): agent layer` carried the commits salvaged here PLUS the RESHAPE and demo overlays that were intentionally dropped. After `part-14/agent-layer-salvage` lands on `main`, PR #53 should be closed (not merged) with a note pointing at the salvage PR. See `release-evidence/branch-cleanup-audit.md` for the disposition of `origin/part-12/agent-layer`.

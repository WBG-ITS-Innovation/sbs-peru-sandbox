# Workflow harness dependencies. Installed separately from the application
# stack (which uses uv from Part 3). Use:
#   pip install -r scripts/requirements-harness.txt
#
# Keep this list short. The harness is meant to be portable.

openai>=2.37.0,<3.0
python-dotenv>=1.2.2,<2.0
gitpython>=3.1.50,<4.0
pyyaml>=6.0.3,<7.0

# Test deps for tests/ (regression tests for the harness scripts).
# Kept in this file so contributors run one install command. When Part 3
# brings uv, the application's dev deps will live in pyproject.toml; these
# harness/test deps stay here.
pytest>=8.0,<9.0
